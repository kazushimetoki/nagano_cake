<% @items.each do |item| %>
  <div>
    <%= link_to item_path(item.id) do %>
     <%= image_tag item.get_image %>
    <% end %>
    <p>商品名：<%= item.name %></p>
    <p>税抜価格：<%= item.price %></p>
  </div>
<% end %>
<%= paginate @items %>